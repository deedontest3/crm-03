// TS-only stub. The tsconfig `paths` entry remaps
// `@/integrations/supabase/client` to this file so callers see a permissive
// `any`-typed client while the auto-generated Database types are still empty.
// At runtime, Vite's `@` -> `./src` alias resolves the same import specifier
// to the real `./client.ts`, so the real Supabase client is used in the
// browser. Do not import this file directly.
export const supabase: any = {};
